# DataHub ML release-audit Skill evidence — 2026-07-27

## Scope

H-Q4 asks whether Hindsight's evidence protocol can become a generally useful DataHub Agent Skill rather than remaining product-specific logic.

## Upstream fit review

The current `datahub-project/datahub-skills` repository uses self-contained packages under `skills/<name>/SKILL.md`, supports Codex and other Agent Skills-compatible clients, prefers MCP with CLI fallbacks, and requires explicit planning/approval for writes. The local contribution follows that shape at `skills/datahub-ml-release-audit/`.

## Delivered contribution

- Trigger-focused `SKILL.md` for target and temporal leakage audits.
- DataHub MCP and CLI evidence-collection workflow.
- Five-state calibrated verdict contract.
- Independent deterministic and point-in-time confirmation routes.
- High-correlation pre-cutoff control requirement.
- Explicit approval and reread requirements for DataHub writeback.
- Standard-library evidence-bundle consistency validator.
- Automated tests for valid and invalid verdict bundles.

## Honest status

The contribution is locally scaffolded, tested, and ready to transplant into an upstream fork. No upstream pull request has been opened from this environment, so H-Q4 is **locally passed / externally pending** rather than claimed complete.
