# Offline fixture replay evidence - 2026-07-27

## Offline replay

`hindsight replay-fixture` completed in **0.022996 seconds** with no external
services. It verified six recorded files against both `manifest.json` and
`hashes.txt`, then reproduced:

- planted leakage verdict: `confirmed`;
- safe-control verdict: `clear_for_release`;
- release decision: `block`;
- transformation status: `violation`;
- remediation status: `safe`;
- recorded `hindsight:leakage-candidate` field tag and upstream direction.

All nine replay checks passed. An altered byte causes `integrity_failed` and
exit code `2` before recorded metadata is evaluated.

## Live semantic-equivalence check

The same fixture was compared with the seeded synthetic DataHub asset. All five
checks passed:

| Check | Result |
|---|---|
| Offline integrity/replay | Pass |
| Expected schema fields in live entity | Pass |
| Candidate field tag in live entity | Pass |
| Target column in live lineage | Pass |
| Upstream source column in live lineage | Pass |

The live field also contained the later `hindsight:leakage-confirmed` and
`hindsight:mcp-verified` tags, which are intentionally not required by the
earlier sanitized recording.

## Recapture safety

`python -m hindsight.fixtures.refresh` is preview-only. Updating the integrity
root requires `--approve-refresh`, followed by a clean replay. The current
preview reports zero changes.
