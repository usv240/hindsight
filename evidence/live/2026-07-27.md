# Live DataHub end-to-end proof — 2026-07-27

This is a sanitized record of a fresh execution against the local synthetic DataHub quickstart. Raw reports remain ignored as `*.local.json`. No production catalog or sensitive data was used.

## Runtime services

| Service | Result |
|---|---|
| DataHub frontend `http://localhost:9002` | HTTP 200 |
| DataHub GMS `http://localhost:8080` | Healthy |
| Standalone DataHub MCP `http://127.0.0.1:8000/mcp` | Connected, server version 3.4.4 |
| Hindsight console `http://127.0.0.1:8100` | Healthy, HTTP 200 |

## Fresh synthetic lineage round trip

Target dataset:

```text
urn:li:dataset:(urn:li:dataPlatform:duckdb,hindsight.phase0.leaky_features_1785182428,PROD)
```

All checks passed:

- dataset schemas were upserted and reread;
- `payment_recorded_at -> days_since_last_payment` fine-grained lineage was reread;
- the `hindsight:leakage-candidate` field tag was reread.

## Independent MCP proof

All checks passed:

- required search, entity, lineage, tag, property, and document tools were exposed;
- MCP search found the newly seeded feature dataset;
- MCP returned its column-level lineage;
- MCP added `hindsight:mcp-verified` to the synthetic field and reread it.

## Approved evidence publication

Hindsight published the `confirmed / block` audit to the synthetic target and reread every mutation:

| Evidence | Reread result |
|---|---|
| `hindsight:leakage-confirmed` field tag | Pass |
| `hindsight.auditVerdict` structured property | Pass |
| Linked audit Document | Pass |
| Active `ML_LEAKAGE` incident | Pass |

The incident URN was `urn:li:incident:b3f096da-2eef-40e6-9e20-c952bc89395f`.

## Judge-console verification

The live `/api/audit` response returned:

- verdict: `confirmed`;
- release decision: `block`;
- release-gate exit code: `3`;
- observed leaked-feature AUC: `1.000000`;
- point-in-time AUC: `0.833630`;
- safe-control AUC: `0.924842`;
- default writeback status: `awaiting_human_approval`.

The server-rendered HTML was checked for the exact `0.21` leaked-feature versus `0.24` safe-control contrast, the “exactly backwards” argument, and the total-by-construction AUC disclosure. All were present. Browser screenshot capture was unavailable because the local browser-control process hit a Windows ACL sandbox error; HTTP, JSON, rendered-HTML, DataHub reread, and MCP verification were unaffected.
