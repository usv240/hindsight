# Approved audit publication evidence - 2026-07-27

The production-shaped publication command was exercised against a synthetic
local DataHub dataset. Raw machine reports remain ignored as `*.local.json`.

## Approval boundary

Running `hindsight publish-audit` without `--approve-writeback`:

- returned `awaiting_human_approval`;
- returned action exit code `2`;
- performed no network call;
- recorded `mutation_performed: false`.

## Approved publication

Target:
`urn:li:dataset:(urn:li:dataPlatform:duckdb,hindsight.phase0.leaky_features_1785173386,PROD)`

| Reread check | Result |
|---|---|
| `hindsight:leakage-confirmed` field tag | Pass |
| `hindsight.auditVerdict = confirmed` structured property | Pass |
| Native audit Document linked to target | Pass |
| Active `CUSTOM / ML_LEAKAGE` incident | Pass |

The resulting document URN is
`urn:li:document:hindsight-audit-credit-default-leaked-payment-event`.

The command was executed a second time. It reused incident
`urn:li:incident:fc2d3271-b8d4-43d0-9e12-f6f5d94e9242`, proving that retries do
not create duplicate active incidents for the same case.

The release decision remains `block`; a successful catalog publication does
not convert a blocking model verdict into a successful release.
